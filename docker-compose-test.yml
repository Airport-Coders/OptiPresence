version: '3.8'
services:
    scaffold-build:
        extends:
            file: common-services.yml
            service: scaffold-build

    scaffold-test:
        build:
            context: ./scaffold-op
            dockerfile: Dockerfile
        command: yarn test
        volumes:
            - ./scaffold-op:/scaffold-op
